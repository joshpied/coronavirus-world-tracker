import styled from 'styled-components';

import Layout from '../components/Layout';

const CountryComparison = styled.main`
  margin-left: 5%;
  margin-right: 5%;
`;

export default function ComparePage(props) {
  return (
    <Layout>
      <CountryComparison>comprarrrreee</CountryComparison>
    </Layout>
  );
}
